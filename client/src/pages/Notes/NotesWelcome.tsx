import { Editor } from "~/components/Editor";

export function NotesWelcome() {
  // const createMutation = useCreateNote();

  return (
    <div>
      NotesWelcome
      <Editor />
    </div>
  );
}
